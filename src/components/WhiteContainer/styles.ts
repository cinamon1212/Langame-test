import { defineStyle, StackProps } from '@chakra-ui/react'

export const whiteSectionStyles = defineStyle({
  p: '30px',
  bgColor: 'white',
  boxShadow: '0px 4px 14px 0px rgba(0, 0, 0, 0.04)',
  borderRadius: '20px',
  alignItems: 'flex-start',
  w: '100%',
}) as StackProps
